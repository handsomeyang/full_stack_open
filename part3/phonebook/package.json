{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook && npm run build && cp -r dist ../../part3/phonebook",
    "deploy:full": "npm run build:ui && git add . && cd ../.. && git commit -am 'phonebook uibuild' && git push"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.2.1",
    "mongoose": "^9.1.1",
    "morgan": "^1.10.1"
  }
}
